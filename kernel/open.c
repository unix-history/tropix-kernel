main (int argc, const char *argv[])
{
	if (argv[1] && open (argv[1], 0) < 0)
		error ("$*erro no open");

	pause ();
#if (0)	/*******************************************************/
#endif	/*******************************************************/

	return (0);
}
